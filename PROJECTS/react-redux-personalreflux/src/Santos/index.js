import React, {Component} from 'react';
import PageToComponent from './PageToComponent';

function BringLight(props) {
    const shedLight = props.santos.map((todo, i) => <PageToComponent
        title={santos.ctemp}
        norris={santos.norris}
        eprice={santos.eprice}
    />);

    return allSantos;
}



export default BringLight;
