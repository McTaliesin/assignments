/*
Make a function that takes a string and returns /
that string with all instances of the letter 'a' /
removed. Must handle lowercase and uppercase letters.



*/
let removeA = function(str){
  let res = str.split("a");
  let tes = str.split("A");
  // combine res and tes into one string and console.log the result
  console.log("res");
  console.log("tes");


}
