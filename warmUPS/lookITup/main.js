//ALES'S SOLUTION
/*var entrries = [];

function Data(word,definition){
  this.word = word;
  this.definition = definition

}

function dictionary(word,definition){
  var newWord = new Data(word,definiton);
  entries.push(newWord);
}

dictionary()
*/

var entrries = {};
entries.alex =  "an english male name"
entreies.brick = "a kiln-fired cubeohexahedron rectangular prism made of muyd used for building";

console.log(entries.brick)

function Data(word,definition){
  this.word = word;
  this.definition = definition;

}

function dictionary(word,definition){
  var newWord = new Data(word,definiton);
  entries.push(newWord);
}

dictionary(anex, an entry room);
dictionary(alex, an english name);
dictionary(apex, peak, top, pinacle);

console
