var twoSum= ([1,2,3], 4);
//returns [0, 2] because 1 + 3 equals 4

var solution = [];
var indexNumOne = [];
var indexNumTwo = [];
var market = indexNumOne + indexNumTwo;
// get value for target integer by subtracting array from string
var target = twoSum - [];
var theArray = twoSum - target;
// test target and it looks like it works, so far so good
console.log(target);
// loop through array: subtract first element from target
// then loop through array to find if target minus next element ===
for (i = 0 ; i < theArray.length; i++ ){

 }

 if (market = target){
  console.log([i, j] = "because " indexNumOne " + " indexNumTwo " = " target);
 }
